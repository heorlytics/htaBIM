# Print method for bim_model

Print method for bim_model

## Usage

``` r
# S3 method for class 'bim_model'
print(x, ...)
```

## Arguments

- x:

  A `bim_model` object.

- ...:

  Further arguments (ignored).
