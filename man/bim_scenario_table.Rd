% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bim_scenario_table.R
\name{bim_scenario_table}
\alias{bim_scenario_table}
\title{Cross-scenario budget impact comparison table}
\usage{
bim_scenario_table(model, years = NULL, currency_millions = TRUE, digits = 2L)
}
\arguments{
\item{model}{A \code{bim_model} object.}

\item{years}{\code{integer} vector. Years to include as columns. If \code{NULL}
(default), uses Year 1, the middle year, and the last year.}

\item{currency_millions}{\code{logical(1)}. Express values in millions.
Default \code{TRUE}.}

\item{digits}{\code{integer(1)}. Decimal places for formatted values. Default
\code{2L}.}
}
\value{
A \code{data.frame} with one row per scenario and columns for each
selected year plus cumulative total, formatted as character strings.
The \code{data.frame} carries a \code{"caption"} attribute suitable for passing
to \code{knitr::kable()}.
}
\description{
Produces a side-by-side summary table of budget impact results across all
scenarios in a model, showing Year 1, mid-point, final year, and cumulative
totals. This is the standard tabular format for dossier submissions following
ISPOR Task Force guidelines.
}
\examples{
pop <- bim_population(
  indication  = "Disease X", country = "custom",
  years = 1:5, prevalence = 0.003, n_total_pop = 42e6,
  diagnosed_rate = 0.60, treated_rate = 0.45, eligible_rate = 0.30
)
ms <- bim_market_share(
  population     = pop,
  treatments     = c("Drug C (SoC)", "Drug A (new)"),
  new_drug       = "Drug A (new)",
  shares_current = c("Drug C (SoC)" = 1.0, "Drug A (new)" = 0.0),
  shares_new     = c("Drug C (SoC)" = 0.8, "Drug A (new)" = 0.2),
  scenarios      = list(
    conservative = c("Drug C (SoC)" = 0.9, "Drug A (new)" = 0.1),
    optimistic   = c("Drug C (SoC)" = 0.7, "Drug A (new)" = 0.3)
  )
)
costs <- bim_costs(
  treatments = c("Drug C (SoC)", "Drug A (new)"),
  drug_costs = c("Drug C (SoC)" = 500, "Drug A (new)" = 25000)
)
model <- bim_model(pop, ms, costs)
st <- bim_scenario_table(model)
print(st)

}
\seealso{
\code{\link[=bim_table]{bim_table()}}, \code{\link[=bim_extract]{bim_extract()}}
}
