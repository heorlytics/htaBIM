% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bim_report.R
\name{bim_report}
\alias{bim_report}
\title{Generate a budget impact model report}
\usage{
bim_report(
  model,
  output_file = NULL,
  format = c("text", "html", "docx"),
  title = NULL,
  author = NULL,
  date = Sys.Date(),
  scenario = "base"
)
}
\arguments{
\item{model}{A \code{bim_model} object.}

\item{output_file}{\code{character(1)} or \code{NULL}. File path for the output
report. If \code{NULL}, returns the report as a character vector (default).}

\item{format}{\code{character(1)}. Output format: \code{"text"} (default), \code{"html"},
or \code{"docx"}. For \code{"html"} and \code{"docx"}, the \code{rmarkdown} and \code{officer}
packages must be installed.}

\item{title}{\code{character(1)} or \code{NULL}. Report title. Defaults to the
model label.}

\item{author}{\code{character(1)} or \code{NULL}. Author name for the report header.}

\item{date}{\code{Date} or \code{character(1)}. Report date. Default \code{Sys.Date()}.}

\item{scenario}{\code{character(1)}. Scenario to report. Default \code{"base"}.}
}
\value{
If \code{output_file} is \code{NULL}, a character vector of report lines.
Otherwise, the path to the written file, invisibly.
}
\description{
Produces a structured text summary report of a budget impact model,
written to a file or returned as a character vector. For Word (.docx)
or HTML output, install the suggested packages \code{officer} and \code{rmarkdown}.
}
\examples{
pop <- bim_population(
  indication    = "IgA Nephropathy",
  country       = "GB",
  years         = 1:3,
  prevalence    = 0.003,
  n_total_pop   = 42e6,
  eligible_rate = 0.30
)
ms <- bim_market_share(
  population     = pop,
  treatments     = c("RASi", "NewDrug"),
  new_drug       = "NewDrug",
  shares_current = c(RASi = 1.0, NewDrug = 0.0),
  shares_new     = c(RASi = 0.8, NewDrug = 0.2)
)
costs <- bim_costs(
  treatments = c("RASi", "NewDrug"),
  drug_costs = c(RASi = 500, NewDrug = 25000)
)
model <- bim_model(pop, ms, costs)

# Return as character vector
rpt <- bim_report(model)
cat(rpt, sep = "\n")

}
