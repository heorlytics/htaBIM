% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bim_costs.R
\name{bim_costs}
\alias{bim_costs}
\title{Build per-patient annual cost inputs for a budget impact model}
\usage{
bim_costs(
  treatments,
  years = 1:5,
  drug_costs = NULL,
  admin_costs = NULL,
  monitoring_costs = NULL,
  ae_costs = NULL,
  other_costs = NULL,
  currency = "GBP",
  price_year = as.integer(format(Sys.Date(), "\%Y")),
  inflation_rate = 0,
  rebates = NULL
)
}
\arguments{
\item{treatments}{\code{character}. Vector of treatment names. Must match those
in \code{\link[=bim_market_share]{bim_market_share()}}.}

\item{years}{\code{integer}. Projection years (default \code{1:5}).}

\item{drug_costs}{Named \code{numeric} vector or \code{NULL}. Annual drug cost per
patient by treatment.}

\item{admin_costs}{Named \code{numeric} vector or \code{NULL}. Annual administration
cost per patient (infusion, injection nurse, etc.).}

\item{monitoring_costs}{Named \code{numeric} vector or \code{NULL}. Annual monitoring
costs (lab tests, clinic visits, imaging).}

\item{ae_costs}{Named \code{numeric} vector or \code{NULL}. Annual adverse event
management costs per patient.}

\item{other_costs}{Named \code{numeric} vector or \code{NULL}. Any other direct
medical costs not captured above.}

\item{currency}{\code{character(1)}. ISO 4217 currency code (e.g. \code{"GBP"},
\code{"USD"}, \code{"EUR"}, \code{"CAD"}). Default \code{"GBP"}.}

\item{price_year}{\code{integer(1)}. Reference price year. Default is the
current calendar year.}

\item{inflation_rate}{\code{numeric(1)}. Annual inflation rate applied to
non-drug costs for years beyond Year 1. Default \code{0.0}.}

\item{rebates}{Named \code{numeric} vector or \code{NULL}. Confidential rebates as
proportions (e.g. \code{c(DrugA = 0.15)} for 15\% rebate). Applied to
\code{drug_costs} only and kept internal (not printed by default).}
}
\value{
An object of class \code{bim_costs}, a list containing:
\describe{
\item{\code{costs}}{A \code{data.frame} with columns \code{treatment}, \code{year},
\code{category}, \code{unit_cost}, \code{total_annual_cost}.}
\item{\code{total}}{A \code{data.frame} with \code{treatment}, \code{year},
\code{total_cost_per_patient}.}
\item{\code{params}}{List of all input parameters (rebates stored but not
printed).}
\item{\code{meta}}{List with \code{currency}, \code{price_year}, \code{treatments}.}
}
}
\description{
Constructs a per-patient annual cost structure for each treatment and cost
category (drug, administration, monitoring, adverse events, other). Supports
optional inflation adjustment, discounting, and confidential rebates.
}
\examples{
costs <- bim_costs(
  treatments = c("RASi", "Sparsentan", "Sibeprenlimab"),
  currency   = "GBP",
  price_year = 2025L,
  drug_costs = c(
    RASi          = 200,
    Sparsentan    = 22000,
    Sibeprenlimab = 28500
  ),
  monitoring_costs = c(
    RASi          = 650,
    Sparsentan    = 1500,
    Sibeprenlimab = 1900
  )
)
print(costs)

}
\seealso{
\code{\link[=bim_costs_drug]{bim_costs_drug()}}, \code{\link[=bim_costs_ae]{bim_costs_ae()}}, \code{\link[=bim_model]{bim_model()}}
}
