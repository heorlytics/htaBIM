% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bim_model.R
\name{bim_model}
\alias{bim_model}
\title{Assemble and run a budget impact model}
\usage{
bim_model(
  population,
  market_share,
  costs,
  payer = bim_payer_default(),
  discount_rate = 0,
  label = NULL
)
}
\arguments{
\item{population}{A \code{bim_population} object from \code{\link[=bim_population]{bim_population()}}.}

\item{market_share}{A \code{bim_market_share} object from \code{\link[=bim_market_share]{bim_market_share()}}.}

\item{costs}{A \code{bim_costs} object from \code{\link[=bim_costs]{bim_costs()}}.}

\item{payer}{A \code{bim_payer} object from \code{\link[=bim_payer]{bim_payer()}} or one of the
pre-built payer functions. Default is \code{\link[=bim_payer_default]{bim_payer_default()}}.}

\item{discount_rate}{\code{numeric(1)}. Annual discount rate applied to Year 2+
costs. Per ISPOR guidelines, the base case should be undiscounted
(\code{0}). Default \code{0}.}

\item{label}{\code{character(1)} or \code{NULL}. Optional model label for reporting.}
}
\value{
An object of class \code{bim_model}, a list containing:
\describe{
\item{\code{population}}{The input \code{bim_population} object.}
\item{\code{market_share}}{The input \code{bim_market_share} object.}
\item{\code{costs}}{The input \code{bim_costs} object.}
\item{\code{payer}}{The input \code{bim_payer} object.}
\item{\code{results}}{A list with \code{annual} and \code{cumulative} data frames.}
\item{\code{meta}}{A list with model metadata.}
}
}
\description{
Combines a \code{\link[=bim_population]{bim_population()}}, \code{\link[=bim_market_share]{bim_market_share()}}, and \code{\link[=bim_costs]{bim_costs()}}
object into a complete budget impact model and computes the annual and
cumulative budget impact across all scenarios.

The budget impact for year \emph{t} is defined as:

\deqn{BI_t = \sum_i N_t \cdot s_i^{new}(t) \cdot c_i(t)
            - \sum_i N_t \cdot s_i^{current}(t) \cdot c_i(t)}

where \eqn{N_t} is the number of eligible patients, \eqn{s_i(t)} is the
market share of treatment \eqn{i}, and \eqn{c_i(t)} is the cost per patient
for treatment \eqn{i}.
}
\examples{
pop <- bim_population(
  indication    = "IgA Nephropathy",
  country       = "GB",
  years         = 1:3,
  prevalence    = 0.003,
  n_total_pop   = 42e6,
  eligible_rate = 0.30
)

ms <- bim_market_share(
  population     = pop,
  treatments     = c("RASi", "Sparsentan", "Sibeprenlimab"),
  new_drug       = "Sibeprenlimab",
  shares_current = c(RASi = 0.75, Sparsentan = 0.25, Sibeprenlimab = 0.00),
  shares_new     = c(RASi = 0.60, Sparsentan = 0.20, Sibeprenlimab = 0.20)
)

costs <- bim_costs(
  treatments = c("RASi", "Sparsentan", "Sibeprenlimab"),
  drug_costs = c(RASi = 200, Sparsentan = 22000, Sibeprenlimab = 28500)
)

model <- bim_model(pop, ms, costs, label = "IgAN BIM")
summary(model)

}
\seealso{
\code{\link[=bim_population]{bim_population()}}, \code{\link[=bim_market_share]{bim_market_share()}}, \code{\link[=bim_costs]{bim_costs()}},
\code{\link[=bim_payer]{bim_payer()}}, \code{\link[=plot.bim_model]{plot.bim_model()}}, \code{\link[=bim_table]{bim_table()}}
}
