% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bim_psa.R
\name{bim_run_psa}
\alias{bim_run_psa}
\alias{print.bim_psa}
\title{Run a probabilistic sensitivity analysis (PSA)}
\usage{
bim_run_psa(
  model,
  n_sim = 1000L,
  prevalence_se = NULL,
  diagnosed_rate_se = NULL,
  treated_rate_se = NULL,
  eligible_rate_se = NULL,
  cost_cv = NULL,
  year = NULL,
  scenario = "base",
  seed = NULL
)

\method{print}{bim_psa}(x, ...)
}
\arguments{
\item{model}{A \code{bim_model} object (base case).}

\item{n_sim}{\code{integer(1)}. Number of Monte Carlo simulations. Default
\code{1000L}.}

\item{prevalence_se}{\code{numeric(1)}. Standard error for prevalence. If \code{NULL}
(default), prevalence is held fixed.}

\item{diagnosed_rate_se}{\code{numeric(1)} or \code{NULL}. SE for diagnosed rate.}

\item{treated_rate_se}{\code{numeric(1)} or \code{NULL}. SE for treated rate.}

\item{eligible_rate_se}{\code{numeric(1)} or \code{NULL}. SE for eligible rate.}

\item{cost_cv}{\code{numeric(1)} or \code{NULL}. Coefficient of variation applied to
all drug costs simultaneously. If \code{NULL}, costs are held fixed.}

\item{year}{\code{integer(1)}. Budget impact year to summarise. Defaults to the
last year in the model.}

\item{scenario}{\code{character(1)}. Scenario to use. Default \code{"base"}.}

\item{seed}{\code{integer(1)} or \code{NULL}. Random seed for reproducibility.}
}
\value{
An object of class \code{bim_psa}: a list with elements:
\describe{
\item{\code{simulations}}{\code{data.frame} with one row per simulation:
\code{sim}, \code{budget_impact}, and the sampled parameter values.}
\item{\code{summary}}{\code{data.frame} with mean, SD, median, and 95 \\% credible
interval of budget impact.}
\item{\code{year}}{The year summarised.}
\item{\code{scenario}}{The scenario used.}
\item{\code{n_sim}}{Number of simulations run.}
\item{\code{base_bi}}{Base-case budget impact for reference.}
}
}
\description{
Performs a Monte Carlo PSA by repeatedly sampling uncertain parameters from
their assumed statistical distributions and re-running the budget impact
model for each draw. This produces a distribution of budget impact outcomes
that reflects joint parameter uncertainty.

\strong{Distributional assumptions}
\itemize{
\item Prevalence, diagnosed rate, treated rate, eligible rate -- \strong{Beta}
distribution parameterised from the base-case value and a standard error.
\item Drug cost -- \strong{LogNormal} distribution parameterised from the
base-case value and a coefficient of variation (CV).
}
}
\examples{
pop <- bim_population(
  indication  = "Disease X", country = "custom",
  years = 1:5, prevalence = 0.003, n_total_pop = 42e6,
  diagnosed_rate = 0.60, treated_rate = 0.45, eligible_rate = 0.30
)
ms <- bim_market_share(
  population     = pop,
  treatments     = c("Drug C (SoC)", "Drug A (new)"),
  new_drug       = "Drug A (new)",
  shares_current = c("Drug C (SoC)" = 1.0, "Drug A (new)" = 0.0),
  shares_new     = c("Drug C (SoC)" = 0.8, "Drug A (new)" = 0.2)
)
costs <- bim_costs(
  treatments = c("Drug C (SoC)", "Drug A (new)"),
  drug_costs = c("Drug C (SoC)" = 500, "Drug A (new)" = 25000)
)
model <- bim_model(pop, ms, costs)
set.seed(1)
psa <- bim_run_psa(model, n_sim = 200L, prevalence_se = 0.0005,
                   eligible_rate_se = 0.05, cost_cv = 0.10)
print(psa)

}
\seealso{
\code{\link[=bim_plot_psa]{bim_plot_psa()}}, \code{\link[=bim_run_dsa]{bim_run_dsa()}}
}
