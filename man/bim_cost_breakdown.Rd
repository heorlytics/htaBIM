% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bim_cost_breakdown.R
\name{bim_cost_breakdown}
\alias{bim_cost_breakdown}
\title{Per-patient cost breakdown by component and treatment}
\usage{
bim_cost_breakdown(model, year = NULL, currency_millions = FALSE, digits = 0L)
}
\arguments{
\item{model}{A \code{bim_model} object.}

\item{year}{\code{integer(1)}. Price year to extract costs for. Defaults to
\code{model$costs$meta$price_year} (base price year, before inflation).}

\item{currency_millions}{\code{logical(1)}. Express values in millions.
Default \code{FALSE} (per-patient costs are typically in whole currency units).}

\item{digits}{\code{integer(1)}. Decimal places. Default \code{0L}.}
}
\value{
A \code{data.frame} with rows = cost categories and columns =
treatments, plus a \strong{Total} row. Values are formatted character strings.
Carries a \code{"caption"} attribute.
}
\description{
Extracts and formats the per-patient annual cost decomposed by cost
category (drug, admin, monitoring, adverse events, other) for each
treatment in the model. This supports transparency and helps reviewers
understand the drivers of differential costs between treatments.

The table is suitable for direct inclusion in HTA dossier appendices.
}
\examples{
pop <- bim_population(
  indication  = "Disease X", country = "custom",
  years = 1:5, prevalence = 0.003, n_total_pop = 42e6,
  diagnosed_rate = 0.60, treated_rate = 0.45, eligible_rate = 0.30
)
ms <- bim_market_share(
  population     = pop,
  treatments     = c("Drug C (SoC)", "Drug A (new)"),
  new_drug       = "Drug A (new)",
  shares_current = c("Drug C (SoC)" = 1.0, "Drug A (new)" = 0.0),
  shares_new     = c("Drug C (SoC)" = 0.8, "Drug A (new)" = 0.2)
)
costs <- bim_costs(
  treatments       = c("Drug C (SoC)", "Drug A (new)"),
  drug_costs       = c("Drug C (SoC)" = 500,  "Drug A (new)" = 25000),
  monitoring_costs = c("Drug C (SoC)" = 200,  "Drug A (new)" = 1500),
  ae_costs         = c("Drug C (SoC)" = 50,   "Drug A (new)" = 300)
)
model <- bim_model(pop, ms, costs)
bim_cost_breakdown(model)

}
\seealso{
\code{\link[=bim_costs]{bim_costs()}}, \code{\link[=bim_costs_drug]{bim_costs_drug()}}, \code{\link[=bim_costs_ae]{bim_costs_ae()}}
}
