% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bim_costs.R
\name{bim_costs_drug}
\alias{bim_costs_drug}
\title{Calculate per-patient drug cost from pack size and dosing schedule}
\usage{
bim_costs_drug(
  treatment,
  list_price_per_pack,
  dose_per_admin,
  admin_per_year,
  units_per_pack = 1,
  wastage_factor = 1,
  body_weight_kg = NULL
)
}
\arguments{
\item{treatment}{\code{character(1)}. Treatment name.}

\item{list_price_per_pack}{\code{numeric(1)}. List price per pack or vial.}

\item{dose_per_admin}{\code{numeric(1)}. Dose per administration (in the units
consistent with pack size).}

\item{admin_per_year}{\code{numeric(1)}. Number of administrations per year.}

\item{units_per_pack}{\code{numeric(1)}. Number of dose units per pack.
Default \code{1}.}

\item{wastage_factor}{\code{numeric(1)}. Factor for vial/pack wastage (e.g. \code{1.0}
for no wastage, \code{1.15} for 15\% wastage). Default \code{1.0}.}

\item{body_weight_kg}{\code{numeric(1)} or \code{NULL}. Mean patient body weight
(kg), if dosing is weight-based. Default \code{NULL}.}
}
\value{
A named \code{numeric} vector of length 1: annual drug cost per patient,
suitable for use in \code{\link[=bim_costs]{bim_costs()}}.
}
\description{
Helper function to derive an annual drug cost per patient from list price,
pack size, dose, and dosing frequency. Supports weight-based dosing.
}
\examples{
sib_cost <- bim_costs_drug(
  treatment       = "Sibeprenlimab",
  list_price_per_pack = 2375,
  dose_per_admin  = 1,
  admin_per_year  = 12,
  units_per_pack  = 1
)
sib_cost

}
