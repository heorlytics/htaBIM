% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bim_plot.R
\name{plot.bim_model}
\alias{plot.bim_model}
\title{Plot a budget impact model}
\usage{
\method{plot}{bim_model}(x, type = c("line", "bar", "tornado", "shares"), ...)
}
\arguments{
\item{x}{A \code{bim_model} object.}

\item{type}{\code{character(1)}. Plot type:
\itemize{
\item \code{"line"} -- annual budget impact over time (default).
\item \code{"bar"} -- grouped bar chart by year and scenario.
\item \code{"tornado"} -- DSA tornado diagram (requires \code{dsa} argument).
\item \code{"shares"} -- market share stacked bar chart.
}}

\item{...}{Additional arguments passed to the specific plot function.}
}
\value{
Called for side effects (plot). Returns \code{x} invisibly.
}
\description{
Dispatcher for the various \code{htaBIM} plot types. Calls the appropriate
plotting function based on the \code{type} argument.
}
\examples{
pop <- bim_population(
  indication    = "IgA Nephropathy",
  country       = "GB",
  years         = 1:5,
  prevalence    = 0.003,
  n_total_pop   = 42e6,
  eligible_rate = 0.30
)
ms <- bim_market_share(
  population     = pop,
  treatments     = c("RASi", "Sparsentan", "Sibeprenlimab"),
  new_drug       = "Sibeprenlimab",
  shares_current = c(RASi = 0.75, Sparsentan = 0.25, Sibeprenlimab = 0.00),
  shares_new     = c(RASi = 0.60, Sparsentan = 0.20, Sibeprenlimab = 0.20)
)
costs <- bim_costs(
  treatments = c("RASi", "Sparsentan", "Sibeprenlimab"),
  drug_costs = c(RASi = 200, Sparsentan = 22000, Sibeprenlimab = 28500)
)
model <- bim_model(pop, ms, costs)
plot(model, type = "line")
plot(model, type = "bar")
plot(model, type = "shares")

}
\seealso{
\code{\link[=bim_plot_line]{bim_plot_line()}}, \code{\link[=bim_plot_bar]{bim_plot_bar()}}, \code{\link[=bim_plot_tornado]{bim_plot_tornado()}},
\code{\link[=bim_plot_shares]{bim_plot_shares()}}
}
