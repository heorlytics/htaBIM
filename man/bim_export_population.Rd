% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bim_funnel.R
\name{bim_export_population}
\alias{bim_export_population}
\title{Export a population funnel to a formatted Excel workbook}
\usage{
bim_export_population(
  pop,
  file = "population_funnel.xlsx",
  snapshot_year = 1L,
  overwrite = FALSE
)
}
\arguments{
\item{pop}{A \code{bim_population} object from \code{\link[=bim_population]{bim_population()}}.}

\item{file}{\code{character(1)}. Output file path including the \code{.xlsx}
extension. The parent directory must already exist.}

\item{snapshot_year}{\code{integer(1)} or \code{NULL}. Year used for the
\code{"Funnel Snapshot"} sheet. Pass \code{NULL} to omit that sheet.
Default \code{1L}.}

\item{overwrite}{\code{logical(1)}. If \code{FALSE} (default) and \code{file} already
exists, an error is raised. Set \code{TRUE} to silently overwrite.}
}
\value{
The file path \code{file}, invisibly. A message is printed on success.
}
\description{
Writes a formatted \code{.xlsx} workbook with three sheets:
\enumerate{
\item \strong{Annual Funnel} -- the full year-by-year patient count table, as a
styled Excel table.
\item \strong{Parameters} -- every model input documented in a two-column table.
\item \strong{Funnel Snapshot} -- a single-year view with percentage retained at
each stage and percentage of total population (highlighted Eligible row).
}

Requires the \strong{openxlsx} package.
}
\examples{
\dontrun{
pop <- bim_population(
  indication     = "Disease X",
  country        = "GB",
  years          = 1:5,
  prevalence     = 0.003,
  n_total_pop    = 42e6,
  diagnosed_rate = 0.60,
  treated_rate   = 0.45,
  eligible_rate  = 0.30,
  extra_filters  = list(second_line_plus = 0.55, biomarker_positive = 0.40)
)
bim_export_population(pop, file = "population_funnel.xlsx")
}

}
\seealso{
\code{\link[=bim_population]{bim_population()}}, \code{\link[=bim_plot_funnel]{bim_plot_funnel()}}
}
