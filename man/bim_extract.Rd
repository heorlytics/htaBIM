% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bim_results.R
\name{bim_extract}
\alias{bim_extract}
\title{Extract tidy results from a budget impact model}
\usage{
bim_extract(model, level = c("annual", "cumulative"), scenario = "all")
}
\arguments{
\item{model}{A \code{bim_model} object.}

\item{level}{\code{character(1)}. Level of aggregation:
\itemize{
\item \code{"annual"} -- annual budget impact by year and scenario (default).
\item \code{"cumulative"} -- cumulative totals by scenario.
}}

\item{scenario}{\code{character} or \code{"all"}. Scenarios to include.
Default \code{"all"}.}
}
\value{
A \code{data.frame}.
}
\description{
Returns a tidy \code{data.frame} of budget impact results from a \code{\link[=bim_model]{bim_model()}}
object, optionally filtered to a specific level of aggregation.
}
\examples{
pop <- bim_population(
  indication    = "Example",
  country       = "GB",
  years         = 1:3,
  prevalence    = 0.003,
  n_total_pop   = 42e6,
  eligible_rate = 0.30
)
ms <- bim_market_share(
  population     = pop,
  treatments     = c("RASi", "NewDrug"),
  new_drug       = "NewDrug",
  shares_current = c(RASi = 1.0, NewDrug = 0.0),
  shares_new     = c(RASi = 0.8, NewDrug = 0.2)
)
costs <- bim_costs(
  treatments = c("RASi", "NewDrug"),
  drug_costs = c(RASi = 500, NewDrug = 25000)
)
model <- bim_model(pop, ms, costs)
bim_extract(model, level = "annual")
bim_extract(model, level = "cumulative")

}
