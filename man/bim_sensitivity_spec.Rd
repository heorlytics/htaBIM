% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bim_sensitivity.R
\name{bim_sensitivity_spec}
\alias{bim_sensitivity_spec}
\title{Specify a deterministic sensitivity analysis for a budget impact model}
\usage{
bim_sensitivity_spec(
  prevalence_range = NULL,
  diagnosed_rate_range = NULL,
  treated_rate_range = NULL,
  eligible_rate_range = NULL,
  new_drug_share_range = NULL,
  drug_cost_multiplier_range = c(0.85, 1.15),
  extra_params = NULL
)
}
\arguments{
\item{prevalence_range}{\code{numeric(2)} or \code{NULL}. Low and high values for
disease prevalence (proportion).}

\item{diagnosed_rate_range}{\code{numeric(2)} or \code{NULL}. Low and high values for
diagnosed rate.}

\item{treated_rate_range}{\code{numeric(2)} or \code{NULL}. Low and high values for
treated rate.}

\item{eligible_rate_range}{\code{numeric(2)} or \code{NULL}. Low and high values for
eligible rate.}

\item{new_drug_share_range}{\code{numeric(2)} or \code{NULL}. Low and high values for
new drug market share (applied uniformly across years).}

\item{drug_cost_multiplier_range}{\code{numeric(2)} or \code{NULL}. Low and high
multipliers applied to the new drug cost (e.g. \code{c(0.85, 1.15)} for
plus/minus 15\%). Default \code{c(0.85, 1.15)}.}

\item{extra_params}{Named \code{list} or \code{NULL}. Additional parameter ranges as
named elements, each a \code{list(label, base, low, high)}.}
}
\value{
An object of class \code{bim_sensitivity_spec}.
}
\description{
Defines parameter ranges for a deterministic sensitivity analysis (DSA)
on a \code{\link[=bim_model]{bim_model()}}. Each parameter is varied individually from its low
to high value while all others are held at their base case value.
}
\examples{
sens <- bim_sensitivity_spec(
  prevalence_range        = c(0.002, 0.005),
  eligible_rate_range     = c(0.20, 0.45),
  new_drug_share_range    = c(0.10, 0.30),
  drug_cost_multiplier_range = c(0.85, 1.15)
)

}
\seealso{
\code{\link[=bim_run_dsa]{bim_run_dsa()}}, \code{\link[=bim_model]{bim_model()}}
}
