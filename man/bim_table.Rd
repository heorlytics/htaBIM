% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bim_results.R
\name{bim_table}
\alias{bim_table}
\title{Generate a formatted budget impact summary table}
\usage{
bim_table(
  model,
  format = c("both", "annual", "cumulative"),
  scenario = "base",
  digits = 0L,
  caption = NULL,
  footnote = NULL
)
}
\arguments{
\item{model}{A \code{bim_model} object.}

\item{format}{\code{character(1)}. Table format: \code{"annual"}, \code{"cumulative"},
or \code{"both"}. Default \code{"both"}.}

\item{scenario}{\code{character(1)}. Scenario to display. Default \code{"base"}.}

\item{digits}{\code{integer(1)}. Rounding digits. Default \code{0}.}

\item{caption}{\code{character(1)} or \code{NULL}. Table caption.}

\item{footnote}{\code{character(1)} or \code{NULL}. Table footnote.}
}
\value{
A \code{data.frame} formatted for display.
}
\description{
Produces a formatted HTML or plain-text summary table of annual and/or
cumulative budget impact, suitable for inclusion in RMarkdown reports or
HTA dossiers.
}
\examples{
pop <- bim_population(
  indication    = "Example",
  country       = "GB",
  years         = 1:3,
  prevalence    = 0.003,
  n_total_pop   = 42e6,
  eligible_rate = 0.30
)
ms <- bim_market_share(
  population     = pop,
  treatments     = c("RASi", "NewDrug"),
  new_drug       = "NewDrug",
  shares_current = c(RASi = 1.0, NewDrug = 0.0),
  shares_new     = c(RASi = 0.8, NewDrug = 0.2)
)
costs <- bim_costs(
  treatments = c("RASi", "NewDrug"),
  drug_costs = c(RASi = 500, NewDrug = 25000)
)
model <- bim_model(pop, ms, costs)
tab <- bim_table(model)
print(tab)

}
