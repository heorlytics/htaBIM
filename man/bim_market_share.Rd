% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bim_market_share.R
\name{bim_market_share}
\alias{bim_market_share}
\title{Specify treatment market shares for a budget impact model}
\usage{
bim_market_share(
  population,
  treatments,
  new_drug,
  shares_current,
  shares_new,
  dynamics = c("constant", "linear", "logistic", "step"),
  uptake_params = NULL,
  scenarios = NULL
)
}
\arguments{
\item{population}{A \code{bim_population} object from \code{\link[=bim_population]{bim_population()}}.}

\item{treatments}{\code{character}. Vector of all treatment names, including the
new drug.}

\item{new_drug}{\code{character(1)}. Name of the new intervention. Must be an
element of \code{treatments}.}

\item{shares_current}{Named \code{numeric} vector. Market shares in the current
scenario (without the new drug). Values must sum to 1 and all be in
\verb{[0, 1]}. Names must match \code{treatments}.}

\item{shares_new}{Named \code{numeric} vector. Market shares in the new scenario
(with the new drug at full uptake). Values must sum to 1. Names must match
\code{treatments}.}

\item{dynamics}{\code{character(1)}. How the new drug's uptake evolves:
\itemize{
\item \code{"constant"} -- \code{shares_new} apply uniformly in all years (default).
\item \code{"linear"} -- new drug ramps linearly from 0 to target share over
\code{uptake_params$ramp_years} years.
\item \code{"logistic"} -- S-curve uptake. Requires \code{uptake_params$year_50pct} and
optionally \code{uptake_params$steepness}.
\item \code{"step"} -- shares_new is a named list with one vector per year.
}}

\item{uptake_params}{\code{list} or \code{NULL}. Parameters controlling uptake dynamics:
\itemize{
\item For \code{"linear"}: \code{list(ramp_years = 3)}.
\item For \code{"logistic"}: \code{list(year_50pct = 2, steepness = 2)}.
}}

\item{scenarios}{Named \code{list} or \code{NULL}. Alternative market share vectors
(named numerics, same structure as \code{shares_new}) for scenario analysis.
E.g. \code{list(conservative = c(...), optimistic = c(...))}.}
}
\value{
An object of class \code{bim_market_share}, a list containing:
\describe{
\item{\code{shares}}{A \code{data.frame} with columns \code{year}, \code{treatment},
\code{scenario}, \code{share}, \code{n_patients}.}
\item{\code{params}}{List of input parameters.}
\item{\code{meta}}{List with \code{treatments}, \code{new_drug}, \code{dynamics}.}
}
}
\description{
Defines how treatment market shares evolve over time, both in the current
scenario (without the new drug) and in the new scenario (with the new drug
introduced). Supports constant, linear ramp, logistic S-curve, and step
uptake dynamics.
}
\examples{
pop <- bim_population(
  indication  = "Disease X",
  country     = "GB",
  years       = 1:5,
  prevalence  = 0.003,
  n_total_pop = 42e6,
  eligible_rate = 0.30
)

ms <- bim_market_share(
  population     = pop,
  treatments     = c("RASi", "Sparsentan", "Sibeprenlimab"),
  new_drug       = "Sibeprenlimab",
  shares_current = c(RASi = 0.75, Sparsentan = 0.25, Sibeprenlimab = 0.00),
  shares_new     = c(RASi = 0.60, Sparsentan = 0.20, Sibeprenlimab = 0.20),
  dynamics       = "linear",
  uptake_params  = list(ramp_years = 3)
)
print(ms)

}
\seealso{
\code{\link[=bim_population]{bim_population()}}, \code{\link[=bim_model]{bim_model()}}
}
