% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bim_population.R
\name{bim_population}
\alias{bim_population}
\title{Estimate the annual eligible patient population for a budget impact model}
\usage{
bim_population(
  indication,
  country = "GB",
  years = 1:5,
  prevalence = NULL,
  incidence = NULL,
  n_total_pop = NULL,
  diagnosed_rate = 1,
  treated_rate = 1,
  eligible_rate = 1,
  growth_rate = 0,
  approach = c("prevalent", "incident", "both"),
  extra_filters = list(),
  eligible_fn = NULL,
  data_source = NULL
)
}
\arguments{
\item{indication}{\code{character(1)}. Name of the disease or indication. Used
in outputs and reports.}

\item{country}{\code{character(1)}. ISO 3166-1 alpha-2 country code (e.g. \code{"GB"},
\code{"US"}, \code{"CA"}, \code{"DE"}). Used to look up built-in population data if
\code{n_total_pop} is \code{NULL}. Use \code{"custom"} to rely solely on \code{n_total_pop}.}

\item{years}{\code{integer}. Vector of projection years (e.g. \code{1:5}). Default
is \code{1:5}.}

\item{prevalence}{\code{numeric(1)} or \code{NULL}. Point prevalence as a proportion
(e.g. \code{0.002} for 0.2\%). Required when \code{approach} is \code{"prevalent"} or
\code{"both"}.}

\item{incidence}{\code{numeric(1)} or \code{NULL}. Annual incidence rate per 100,000.
Required when \code{approach} is \code{"incident"} or \code{"both"}.}

\item{n_total_pop}{\code{numeric(1)} or \code{NULL}. Total reference population size.
If \code{NULL} and \code{country} is recognised, uses built-in population data.}

\item{diagnosed_rate}{\code{numeric(1)}. Proportion of prevalent/incident cases
that are diagnosed. Must be in \verb{[0, 1]}. Default \code{1.0}.}

\item{treated_rate}{\code{numeric(1)}. Proportion of diagnosed patients receiving
any systemic treatment. Must be in \verb{[0, 1]}. Default \code{1.0}.}

\item{eligible_rate}{\code{numeric(1)}. Proportion of treated patients eligible
for the new drug (e.g. meeting label criteria). Must be in \verb{[0, 1]}.
Default \code{1.0}.}

\item{growth_rate}{\code{numeric(1)}. Annual growth rate applied to the total
population (e.g. \code{0.005} for 0.5\% per year). Default \code{0.0}.}

\item{approach}{\code{character(1)}. Population approach: \code{"prevalent"} (stock
population), \code{"incident"} (new cases per year), or \code{"both"} (sum of
prevalent and incident). Default \code{"prevalent"}.}

\item{extra_filters}{\code{list} or \code{NULL}. Optional named list of additional
filtering proportions applied sequentially \strong{after} \code{eligible_rate}.
Each element must be a single numeric in \verb{[0, 1]}. The names become
column labels in the output (e.g.
\code{list(prior_therapy_failure = 0.6, biomarker_positive = 0.4)}).
Ignored if \code{eligible_fn} is supplied.}

\item{eligible_fn}{\code{function} or \code{NULL}. Optional custom function that
\strong{replaces} the entire \code{treated_rate * eligible_rate * extra_filters}
calculation. Must accept three arguments: \code{n_diagnosed} (numeric vector,
one value per year), \code{n_treated} (numeric vector), and \code{params} (the list
of all scalar inputs). Must return a numeric vector of the same length.
Use this when your eligibility logic is not simple sequential
multiplication (e.g. additive components, minimum/cap rules, lookup
tables). See the examples below.}

\item{data_source}{\code{character(1)} or \code{NULL}. Citation for the epidemiology
data, appended to outputs. Optional.}
}
\value{
An object of class \code{bim_population}, which is a list containing:
\describe{
\item{\code{annual}}{A \code{data.frame} with columns \code{year}, \code{n_total_pop},
\code{n_prevalent_or_incident}, \code{n_diagnosed}, \code{n_treated}, and
\code{n_eligible}. When \code{extra_filters} are supplied, one additional column
per filter step is inserted before \code{n_eligible}, showing the
intermediate patient count at each stage.}
\item{\code{params}}{A list of all input parameters.}
\item{\code{meta}}{A list with \code{indication}, \code{country}, \code{approach},
\code{data_source}.}
}
}
\description{
Builds a year-by-year estimate of the number of patients eligible for a new
treatment, using an epidemiology-driven funnel approach aligned with ISPOR
Task Force guidelines (Sullivan et al., 2014). Supports prevalent, incident,
or combined population approaches.
}
\examples{
# Standard funnel
pop <- bim_population(
  indication     = "Disease X",
  country        = "GB",
  years          = 1:5,
  prevalence     = 0.003,
  n_total_pop    = 42e6,
  diagnosed_rate = 0.60,
  treated_rate   = 0.45,
  eligible_rate  = 0.30
)
print(pop)
summary(pop)

# Extra filters: add line-of-therapy and biomarker criteria after eligible_rate
pop2 <- bim_population(
  indication     = "Disease X",
  country        = "GB",
  years          = 1:5,
  prevalence     = 0.003,
  n_total_pop    = 42e6,
  diagnosed_rate = 0.60,
  treated_rate   = 0.45,
  eligible_rate  = 0.30,
  extra_filters  = list(second_line_plus = 0.55, biomarker_positive = 0.40)
)
pop2$annual

# Custom formula: eligible = max of two additive subgroups, capped at treated
pop3 <- bim_population(
  indication     = "Disease X",
  country        = "GB",
  years          = 1:5,
  prevalence     = 0.003,
  n_total_pop    = 42e6,
  diagnosed_rate = 0.60,
  treated_rate   = 0.45,
  eligible_fn    = function(n_diagnosed, n_treated, params) {
    subgroup_a <- n_diagnosed * 0.20
    subgroup_b <- n_diagnosed * 0.15
    pmin(subgroup_a + subgroup_b, n_treated)
  }
)

}
\references{
Sullivan SD, Mauskopf JA, Augustovski F et al. (2014). Budget impact
analysis--principles of good practice: report of the ISPOR 2012 Budget
Impact Analysis Good Practice II Task Force. \emph{Value Health}, 17(1):5-14.
\doi{10.1016/j.jval.2013.08.2291}
}
\seealso{
\code{\link[=bim_market_share]{bim_market_share()}}, \code{\link[=bim_costs]{bim_costs()}}, \code{\link[=bim_model]{bim_model()}}
}
